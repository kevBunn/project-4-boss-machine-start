## Boss Machine Setup

1. Unzip
2. Navigate into the unzipped folder
3. Run `npm install`
4. Run `npm start`
5. App opens at `http://localhost:3000`
6. Backend runs at `http://localhost:4001`

Make sure Node.js is installed. No other setup required.